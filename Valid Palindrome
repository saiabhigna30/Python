class Solution:
    def isPalindrome(self, s: str) -> bool:
        a=s.lower()
        b="".join(i for i in a if i .isalnum())
        c=b[::-1]
        if c==b:
           return True
        else:
            return False
